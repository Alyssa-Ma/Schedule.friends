import React, {useState} from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SearchList = ({usernames}) => {

    return (
        <Text>Search List</Text>
    )
}

export default SearchList;